---
import '../styles/global.css';

interface Props {
    title: string;
}

const {title} = Astro.props;
---

<!DOCTYPE html>
<html lang="sv">
    <head>
        <meta charset="UTF-8"/>
        <meta name="description" content="Adam Skoglund - Architectural Portfolio" />
        <meta name="keyword" content="Arkitektur Teknik, Architecture Portfolio, Architecture and Engineering" />
        <meta name="viewport" content="width=device-width" />

        <link rel="icon" type="image/svg+xml" href="/"/>
        <link rel="stylesheet" href="global.css">

        <title>{title}</title>
    </head>
    <body>
        <header class="site-header">
            <div class="header-left">
                <a href="/" class="site-title">Adam Skoglund</a>
            </div>
            <div class="header-right">
                <a href="/cv" class="meta-text nav-link">CV / Contact</a>
            </div>
        </header>

        <main>
            <slot />
        </main>
    </body>
</html>

<style>
    .site-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
         padding: var(--spacing-md) var(--spacing-sm);
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        background: rgba(255,255,255,0.9);
        z-index: 100;
        border-bottom: 1px solid transparent;
    }

    .site-title {
        font-family: var(--font-title);
        font-weight: 700;
        font-size: 1.2rem;
        text-transform: uppercase;
    }

    .nav-link:hover {
        text-decoration: underline;
    }

    main {
        padding-top: 100px;
        padding-left: var(--spacing-sm);
        padding-right: var(--spacing-sm);
        max-width: var(--container-max);
        margin: 0 auto;
    }

    @media (min-width: 768px) {
        .site-header {
            padding: var(--spacing-md) var(--spacing-xl);
        }
        main {
            padding-left: var(--spacing-xl);
            padding-right: var(--spacing-xl);
        }
    }

</style>
