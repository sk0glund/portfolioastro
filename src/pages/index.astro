---
import Layout from '../layouts/Layout.astro';

/* Placeholder projekt */
const projects = [
	{
		id: 1,
		name: 'Rimfrost',
		year: '2025',
		type: 'Explorative project',
		image: "projects/1/optimerade-rendering1.jpg"
	},
	{
		id: 2,
		name: 'Lamp - Parametric Design',
		year: '2023',
		type: 'Design Project',
		image: "/projects/2/lamp.jpg"
	},
	{
		id: 3,
		name: 'Radhus',
		year: '2025',
		type: 'Residential',
		image: "/material.png"
	}
]

const featuredProject = projects[1];

---

<Layout title="Adam Skoglund | Portfolio ">
	<section class="hero-section">
		<a href={`projects/${featuredProject.id}`} class="hero-link">
			<div class="hero-image-container">
				<img src={featuredProject.image} alt={featuredProject.name} class="hero-img" />
				
				<div class="hero-overlay">
					<h2 class="hero-title">{featuredProject.name}</h2>
					<span class="meta-text">Show Project</span>
				</div>
			</div>
		</a>
	</section>

	<section class="project-list">
		<div class="list-header meta-text">
			<span>Project</span>
			<span>Year / Category</span>
		</div>
		
		<hr />

		<ul>
			{projects.map((project) => (
				<li>

					<a href={`project/${project.id}`} class="project-row">
						<span class="project-name">{project.name}</span>
						<span class="project-meta">{project.year} - {project.type}</span>
					</a>

				</li>
			))}

		</ul>
	</section>

</Layout>

<style>

/* Hero style*/
	.hero-section {
		margin-bottom: var(--spacing-lg);
		height: 70vh;
		width: 100%;
	}

	.hero-link {
		display: block;
		height: 100%;
		width: 100%;
		position: relative;
	}

	.hero-image-container {
		width: 100%;
		height: 100%;
		position: relative;
		overflow: hidden;
		background-color: #f0f0f0;	
	}

	.hero-img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: transform 0.6s ease;
	}

	.hero-overlay {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: rgba(255, 255, 255, 0.0);
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		opacity: 0;
		transition: all 0.4s ease;
	}

	.hero-link:hover .hero-overlay {
		background: rgba(255, 255, 255, 0.3);
		opacity: 1;
		cursor: pointer;
	}

	.hero-link:hover .hero-img {
		transform: scale(1.02);
	}

	.hero-title {
		font-size: 3rem;
		color: #000;
		margin-bottom: 1rem;
	}

	/* Projectlist Style */
	.project-list {
		margin-bottom: var(--spacing-lg);
	}

	.list-header {
		display: flex;
		justify-content: space-between;
		margin-bottom: 0.5rem;
		opacity: 0.6;
	}

	hr {
		border: 0;
		border-top: 1px solid #000;
		margin-bottom: 1rem;
	}

	ul {
		list-style: none;
	}

	.project-row {
		display: flex;
		justify-content: space-between;
		align-items: baseline;
		padding: 1rem 0;
		border-bottom: 1px solid var(--color-border);
		transition: opacity 0.2s;
	}

	.project-row:hover {
		opacity: 0.6;
	}

	.project-name {
		font-size: 1.5rem;
		font-weight: 500;
	}

	.project-meta {
		font-size: 0.9rem;
	}

	@media (max-width: 768px) {
		.hero-title {
			font-size: 2rem;
		}
		.project-name {
			font-size: 1.2rem;
		}
	}
</style>
